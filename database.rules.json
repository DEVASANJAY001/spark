{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "typing": {
      "$matchId": {
        "$uid": {
          ".write": "auth != null && auth.uid == $uid",
          ".read": "auth != null"
        }
      }
    }
  }
}